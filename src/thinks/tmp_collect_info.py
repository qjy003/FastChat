from src.base.think import ThinkModulefrom typing import Anyfrom src.utils import trim_stringfrom src.utils import output_json_dataclass TmpCollectInfo(ThinkModule):    """    仅根据客户回复的最新内容来提取物流信息的信息收集模块    """    def initialize_prompt(self) -> str:        """        填写用于提取物流信息的提示词        Returns: 提取物流信息的提示词        """        return \            """    #角色你是京东物流的资深营销专家，你能够清晰地理解文本中隐晦的客户信息，包括{collect_info}这{collect_info_num}项信息。#输入我们会提供用3个等号(===)包围的完整的对话内容#任务1. 你必须认真遵守注意事项当中的内容，以字典的形式返回结果，且必须只包含data字段，且data字段对应的值也是一个字典.2. 你首先需要在data字段对应的字典内添加{collect_info}这{collect_info_num}个字段，且先将每个字段对应的值设置为空字符串。3. 注意User若只是表示某项信息出现错误，但未给出具体的信息时，将相应的字段记为空字符串。{info_check_laws}#注意事项仅根据对话历史当中3个等号(===)包裹的完整对话内容来获取User提供的关于{collect_info}这{collect_info_num}项信息的值，还要充分利用User提供的"补充信息"部分的内容补全要收集的{collect_info_num}项信息。保存在data字段。注意，你需要收集的信息项名称，分别为{collect_info}注意，从User输入中提供的该项信息，没有提供的话则值为'',不要填写其它值。注意，不要扩充字段的数量，只返回这{collect_info_num}项基本信息的键值。注意，User提供的信息可能不属于要收集的{collect_info_num}项信息范围，要注意判断。注意，一定要输出data字段，且data字段当中一定要包含{collect_info}这{collect_info_num}个字段，不可为空注意，仅能使用3个等号(===)包裹的内容来进行分析生成data字段的内容，不能使用其它的信息#对话历史==={conversation_history}===#任务1. 你必须认真遵守注意事项当中的内容，以字典的形式返回结果，且必须只包含data字段，且data字段对应的值也是一个字典.2. 你首先需要在data字段对应的字典内添加{collect_info}这{collect_info_num}个字段，且先将每个字段对应的值设置为空字符串。3. 注意User若只是表示某项信息出现错误，但未给出具体的信息时，将相应的字段记为空字符串。{info_check_laws}    """    def process_inference_results(self, inference_results: Any) -> Any:        """        处理大模型执行推理所返回的结果，并将处理后的结果返回给对话应用        Args:            inference_results: 大模型执行推理所返回的结果        Returns: 处理后的结果返回给对话应用的结果        """        json_results_str = trim_string(inference_results)        json_results = output_json_data(json_results_str)        return json_results